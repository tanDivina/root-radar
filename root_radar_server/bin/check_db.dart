import 'package:serverpod/serverpod.dart';
import 'src/generated/protocol.dart';

Future<void> main() async {
  // This is a hacky way to run a script within the server's context
  // But wait, it's better to just use a script that connects to the DB
}
